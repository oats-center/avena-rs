# Avena LabJack Pipeline Webpage

This folder is a static GitHub Pages-ready guide for the repo.

Open `index.html` directly, or serve the folder with any static server:

```bash
python3 -m http.server 8080 --directory webpage
```

The demo video is stored at `assets/avena-recording.mov`.
